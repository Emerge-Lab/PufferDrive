benchmarks:
  # Gigaflow - Multi agent only
  - name: carla
    mode: gigaflow
    num_scenarios: 250
    num_scenarios_to_render: 8
    num_maps: 8
    max_agents_per_env: 50
    scenario_length: 2000
    control_mode: control_vehicles
    paths:
      local: pufferlib/resources/drive/binaries/carla

  - name: handball
    mode: gigaflow
    num_scenarios: 100
    num_scenarios_to_render: 1
    num_maps: 1
    max_agents_per_env: 100
    scenario_length: 3000
    control_mode: control_vehicles
    paths:
      local: pufferlib/resources/drive/binaries/handball

  - name: womd_multi
    mode: replay
    num_scenarios: 100
    num_scenarios_to_render: 10
    scenario_length: 91
    control_mode: control_vehicles
    paths:
      local: pufferlib/resources/drive/binaries/womd

  - name: womd_single
    mode: replay
    num_scenarios: 100
    num_scenarios_to_render: 10
    scenario_length: 91
    control_mode: control_sdc_only
    paths:
      local: pufferlib/resources/drive/binaries/womd

  # # nuPlan - Multi agent / Single agent
  - name: nuplan_multi
    mode: replay
    num_scenarios: 100
    num_scenarios_to_render: 10
    scenario_length: 91
    control_mode: control_vehicles
    paths:
      local: pufferlib/resources/drive/binaries/nuplan

  - name: nuplan_single
    mode: replay
    num_scenarios: 100
    num_scenarios_to_render: 10
    scenario_length: 91
    control_mode: control_sdc_only
    paths:
      local: pufferlib/resources/drive/binaries/nuplan
